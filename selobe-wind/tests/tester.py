import logging, time

if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)
    
    logging.info("Running multiple tests...")
    time.sleep(10)
    logging.info("Finished running multiple tests.")